# Обход (exploration): инкрементальный экспертный бэклог

Документ для поэтапной доводки 3D-обхода без «магии в одном PR». Статусы: **сделано** | **в работе** | **запланировано**.

## Слой 0 — стабильность движка (сделано в текущей итерации)

| ID | Проблема | Решение / файлы |
|----|------------|-----------------|
| E0.1 | WASD не от орбиты камеры | `explorationCameraOrbitBridge.ts`, `FollowCamera.tsx`, `PhysicsPlayer.tsx`, `ExplorationNoclipPlayer.tsx` |
| E0.2 | Падение при выходе в коридор / за край пола | `explorationNarrativeTeleport.ts`, дверные хендлеры, расширение `PhysicsFloor` в `PhysicsSceneColliders.tsx`, спавн-снимок в `RPGGameCanvas.tsx` |
| E0.3 | Камера далеко для «Gothic / WoW» | Пропсы `followCameraProps` в `RPGGameCanvas.tsx` |

## Слой 1 — визуал персонажа и NPC

| ID | Задача | Проверка готовности |
|----|--------|---------------------|
| E1.1 | «Гигант в центре» (T-pose): аудит `useGLTF` / общий root без clone, дубли `primitive`, масштаб при `hRaw` | `NEXT_PUBLIC_EXPLORATION_MESH_AUDIT=1`, сравнение bbox с капсулой |
| E1.2 | Анимации игрока: отдельный Walk/Run клип или blend tree; без «дёрганья» при смене isMoving | Визу + `stripExplorationPlayerRootMotionFromClips` |
| E1.3 | Анимации NPC: корректные имена клипов в `AnimationMapping`, walk по патрулю без скольжения | `npcDefinitions.ts`, `NPC.tsx` |
| E1.4 | Имена над NPC + «!» при активном квесте | UI billboards / drei `Html`, связка с `quests` / `emitQuestEvent` |

## Слой 2 — геймплей и смысл

| ID | Задача | Примечание |
|----|--------|------------|
| E2.1 | Квесты и карма: одна строка в `ExplorationBriefingOverlay` + тост при первом диалоге | Не лор-стена: «карма влияет на тон диалогов и ветки» |
| E2.2 | Предметы: подбор / осмотр / ключевые `itemId` | Уже частично через `object:interact`; выровнять радиальное меню |
| E2.3 | Инвентарь: зачем слоты, «использовать» в обходе | Минимум: 1–2 расходника с тостом эффекта |
| E2.4 | Комната «пустая» — наполнение без новых GLB | Процедурные детали, свет, 1–2 читаемых якоря взгляда |

## Слой 3 — UX и платформы

| ID | Задача |
|----|--------|
| E3.1 | Подсказка управления: камера мышью / тач, R — сброс орбиты |
| E3.2 | Мобильные малые экраны: safe-area, размер кнопок `ExplorationMobileHud` |
| E3.3 | Тач-орбита камеры: чувствительность, зона не перекрывающая джойстик |

## Слой 4 — нарратив и заставки

| ID | Задача |
|----|--------|
| E4.1 | Интро: лифт 10→3, квартира Заремы и Альберта, стилистика как меню | Отдельный поток `requestCutscene` / новый `animeCutscenes` или HTML cinematic |
| E4.2 | Входная история в 2–3 предложения в брифинге первого визита | Согласовать с `verticalSliceStoryNodes` |

## Слой 5 — регресс и выкладка

| ID | Задача |
|----|--------|
| E5.1 | Прогон `npx tsc --noEmit`, `npx vitest run`, ручной проход: комната ↔ коридор ↔ home |
| E5.2 | `CHANGELOG.md` + `git push origin` для Vercel |

---

**Порядок внедрения (рекомендация):** E0 → E1.1–E1.2 → E2.1 → E3.1 → E1.4 → E4.1 → E5.
