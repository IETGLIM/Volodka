/**
 * Canonical collectible poem count for golden-path endings and dialogue gates.
 * Must stay in sync with UNIFIED_POEM_REGISTRY — enforced in contentPipelineValidator.
 * Kept dependency-free so goldenPath / chk dialogues never pull the poems chunk.
 */
export const TOTAL_UNIFIED_POEMS = 46;
