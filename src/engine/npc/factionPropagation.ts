/* Re-export from shared bridge — original implementation moved to avoid shared→engine import. */
export { propagateFactionRelationChange, FACTION_PROPAGATION_FACTOR } from '@/shared/gameBridge/factionBridge';
