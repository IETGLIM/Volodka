'use client';

/**
 * Шаг 5: отдельного менеджера списка NPC в `components/3d` нет — экземпляры рендерятся из
 * **`RPGGameCanvas`** / оркестратора; **LOD и видимость модели** — внутри **`NPC`**.
 *
 * См. **`LODController.tsx`** и **`lib/npcLodConstants.ts`**.
 */

export {};
