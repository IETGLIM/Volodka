.vercel
.env*.local
browserbase-functions/**/.env
.browserbase

node_modules
.next
out
dist
coverage
.turbo
playwright-report
test-results
tsconfig.tsbuildinfo
_volodka_upstream_check

# Логи локального dev (`npm run dev` с tee и т.п.)
*.log

# Архив для Blender / анализа GLB (генерируется scripts/pack-model-blender-analysis.ps1)
exports/volodka-models-blender-analysis.zip
exports/volodka-models-blender-analysis/
exports/volodka-model-scale-analysis-bundle-*.zip
exports/volodka-model-scale-analysis-bundle-*/

# Локальные сборки для коллег (генерируются скриптом)
archives/*.zip
archives/_stage_expert_review_*

# Снимки/дампы репозитория в корне (не версионировать)
/volodka-aaa-current.zip

# Записи Performance (Chrome DevTools), часто сотни МБ — не в репозиторий
Trace-*.json

# Записи экрана в корне (Windows «Запись экрана … .mp4») — только локально
/Запись экрана*.mp4

# Локальный архив проекта в public (не в git)
/public/Volodka-main.zip
